ユースケース:チェックインする

アクタ:ホテルの受付係(管理者)

目的:ホテルの受付係(管理者)がユーザのチェックイン手続きを行う

事前条件:ユーザから予約番号を聞く

事後条件:ユーザがチェックイン済みになる、アクタが部屋番号を取得している。

基本系列
    1. アクタはシステムにチェックイン手続きをする旨を示す
    2. システムはアクタに予約番号の入力を促す
    3. アクタはシステムに予約番号を入力する
    4. システムは予約番号の一致する予約を検索し、部屋番号を出力する。

代替系列（なし）

備考（なし）
# 実装・保守パートの発表案

## 1．授業資料との対応

授業資料を確認すると，今回の発表には次の条件がある。

- チーム発表は3分以内であり，実装デモを含める。
- 作業項目は層ごとに分断せず，ユースケース単位で画面からDBまで一貫して実装する。
- ドメイン分析，要求分析，システム分析から実装までの対応を示す。
- GitHubで進捗と成果物を管理する。
- レビューとチェックリストによって欠陥を確認する。

このため，実装・保守パートは2枚と短いデモで構成する。チーム全体が3分の場合，このパートはデモを含めて約90秒を目安とする。

## 2．スライド1

### タイトル

**6つのユースケースをWeb上で一貫して実装**

### 掲載内容

左側に，実装したユースケースを簡潔に載せる。

- 部屋を予約する
- 自分の予約一覧を確認する
- 予約をキャンセルする
- 受付係が全予約を確認する
- チェックインする
- チェックアウトする

右側に，次の4層を縦に並べた簡略図を載せる。

```text
Webブラウザ
      ↓
app.web
画面・HTTP処理，boundary
      ↓
Control
ユースケースの進行，control
      ↓
Domain
予約規則・状態遷移，entity
      ↓
JDBC DAO → HSQLDB
永続化
```

下部に，開発管理を1行で載せる。

> 要求分析 → ユースケース単位で実装 → 作業ブランチ → PR #6 → mainへ統合

### 見せ方

- 既存の詳細なパッケージ図をそのまま縮小して載せず，上記の4層だけを大きく示す。
- 6機能は小さなアイコンまたは2列の箇条書きにする。
- クラス名を大量に並べず，分析モデルと実装層の対応を中心にする。

## 3．スライド2

### タイトル

**層分離と自動テストによって変更の影響を限定**

### 掲載内容

#### 保守しやすい責務分担

- 画面変更は`app.web`に限定する。
- 予約規則は`domain`に集約する。
- DB処理はDAOとJDBC実装に限定する。

#### データ不整合を防ぐ仕組み

```text
RESERVED → CHECKED_IN → CHECKED_OUT
    └────────→ CANCELED
```

- 更新処理をトランザクション化し，失敗時にロールバックする。
- 部屋番号と宿泊日のDB制約によって二重予約を防止する。

#### 検証結果

- `ReservationStateTest`：予約状態の正常遷移と不正遷移を検証
- `HotelReservationIntegrationTest`：満室，キャンセル，再割当，所有者確認，チェックイン・アウト，料金6000円を検証
- `WebHttpSmokeTest`：主要画面，予約フォーム，予約一覧のHTTP応答を検証
- 2026年7月9日の実行結果：3件すべて成功

### レビュー時に示すチェック項目

- HandlerがDAOを直接呼んでいない。
- ControlがHTMLを生成していない。
- JDBC APIの利用をDAO実装とJDBC基盤へ限定している。

## 4．デモの操作順

デモは約25秒で，利用者から受付係までのエンドツーエンド動作を見せる。

1. トップ画面から「部屋を予約する」を開く。
2. 当日をチェックイン日として予約し，予約番号が表示されることを示す。
3. 受付係の予約一覧を開き，先ほどの予約が表示されることを示す。
4. 「チェックイン」を実行し，状態と部屋番号が表示されることを示す。

チェックアウトは翌日だけ許可されるため，同日のライブデモでは実行しない。翌日のチェックアウトと料金6000円は統合テストで検証済みであると説明する。

## 5．発表原稿

ここから，実装と保守について説明します。

授業で示されたドメイン分析から実装までの流れに沿い，要求分析で定義した6つのユースケースをWeb機能として実装しました。具体的には，予約，自分の予約確認，キャンセル，受付係による全予約確認，チェックイン，チェックアウトです。作業は画面やDBといった層ごとに分断せず，ユースケースごとにブラウザ操作からDB保存まで動作する単位で進めました。実装は作業ブランチで行い，PR #6としてmainへ統合しました。

構成は，分析モデルのboundary，control，entityに対応する4層構成です。`app.web`はHTTP入出力，Controlはユースケースの進行，Domainは予約規則と状態遷移，DAOはHSQLDBへの保存を担当します。この分離により，例えば画面を変更しても，予約規則やDB処理への影響を限定できます。

保守面では，予約状態を列挙型で管理し，許可された状態遷移だけを実行できるようにしました。また，更新処理をトランザクション化し，部屋番号と宿泊日のDB制約によって二重予約を防止しています。さらに，レビュー項目として，HandlerがDAOを直接呼ばないこと，ControlがHTMLを生成しないこと，JDBCの利用範囲を限定していることを確認しました。

最後に，状態遷移，主要ユースケース，Web画面を対象とする3つの自動テストを実行し，すべて成功しています。それでは，予約から受付係の確認までをデモします。

## 6．デモ中の短い原稿

利用者として，当日分の部屋を予約します。入力を送信すると，予約番号と割り当てられた部屋が表示されます。次に受付係の予約一覧を開くと，同じ予約が確認できます。ここでチェックインを実行すると，予約状態が「チェックイン済み」に変わり，部屋番号が表示されます。このように，要求分析で定義したユースケースを画面からDBまで一貫して動作させています。

## 7．発表時の注意

- 「GitHub Issueで管理した」とは，実際のIssueが確認できる場合だけ述べる。
- PR #6によるブランチ統合はGit履歴で確認できるため，発表に載せてよい。
- 「レビューした」と述べる場合は，上記3項目を発表前にチームで再確認する。
- スライドにはソースコードを載せず，4層構成，状態遷移，テスト結果を視覚的に示す。
- 3分発表では，機能をすべて操作せず，予約からチェックインまでの代表シナリオだけを見せる。
